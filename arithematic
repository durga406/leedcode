#include <stdio.h>
int gcd(int a, int b) {
    while (b != 0) {
        int temp = b;
        b = a % b;
        a = temp;
        }
        return a;
}
int main(){
  int a,b,c,d;
  scanf("%d%d%d%d",&a,&b,&c,&d);
  int t=gcd(12, 18)+gcd(24, 36)+gcd(12, 24)+gcd(18, 36);
  printf("%d",t);
}
